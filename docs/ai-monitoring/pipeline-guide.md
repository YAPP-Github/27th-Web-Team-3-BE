# AI 모니터링 파이프라인 운영 가이드

> 최종 업데이트: 2026-02-03
> 상태: **운영 중**

## 개요

에러 발생부터 자동 수정 PR 생성까지의 전체 파이프라인입니다.

---

## 파이프라인 흐름도

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         🚀 AI 모니터링 파이프라인                             │
└─────────────────────────────────────────────────────────────────────────────┘

                              [Rust 서버]
                                   │
                                   ▼
                         ┌─────────────────┐
                         │  JSON 에러 로그  │
                         │ logs/server.*.log│
                         └────────┬────────┘
                                  │
                    ┌─────────────▼─────────────┐
                    │     Log Watcher (Cron)    │
                    │      매일 09:00 실행       │
                    │    scripts/log-watcher.sh │
                    └─────────────┬─────────────┘
                                  │
                    ┌─────────────▼─────────────┐
                    │      에러 심각도 분류       │
                    └─────────────┬─────────────┘
                                  │
          ┌───────────────────────┼───────────────────────┐
          │                       │                       │
          ▼                       ▼                       ▼
   ┌─────────────┐        ┌─────────────┐        ┌─────────────┐
   │  CRITICAL   │        │    HIGH     │        │ WARNING/INFO│
   │ AI5001-5003 │        │  DB5001+    │        │  AUTH4001+  │
   └──────┬──────┘        └──────┬──────┘        └──────┬──────┘
          │                      │                      │
          └──────────┬───────────┘                      │
                     │                                  ▼
                     ▼                            [로그만 기록]
          ┌─────────────────┐
          │   AI 진단 실행   │
          │  OpenAI GPT-4o  │
          │   mini 모델     │
          └────────┬────────┘
                   │
          ┌────────▼────────┐
          │  Discord 알림   │
          │  (상세 진단 포함) │
          └────────┬────────┘
                   │
          ┌────────▼────────┐
          │  GitHub Issue   │
          │    자동 생성     │
          └────────┬────────┘
                   │
                   ▼
          ┌─────────────────┐
          │  auto_fixable?  │
          └────────┬────────┘
                   │
         ┌─────────┴─────────┐
         │ YES               │ NO
         ▼                   ▼
  ┌─────────────┐     ┌─────────────┐
  │ Auto-Fix PR │     │  수동 수정   │
  │  Draft 생성  │     │   필요      │
  └──────┬──────┘     └─────────────┘
         │
         ▼
  ┌─────────────┐
  │  CI/CD 실행  │
  │ pr-check.yml│
  └──────┬──────┘
         │
         ▼
  ┌─────────────┐
  │ Discord 알림 │
  │  PR 생성됨   │
  └─────────────┘
```

---

## 에러 심각도 분류

| 심각도 | 에러 코드 | 대응 |
|--------|----------|------|
| **CRITICAL** | `AI5001`, `AI5002`, `AI5003`, `AI5031`, `COMMON500` | AI 진단 + Issue + Discord |
| **HIGH** | `DB5001+`, `EXT5001+` | AI 진단 + Issue + Discord |
| **WARNING** | `AUTH4001-4005` | Discord 알림만 |
| **INFO** | 기타 | 로그만 기록 |

---

## 스크립트 목록

| 스크립트 | 역할 | 실행 방법 |
|----------|------|----------|
| `log-watcher.sh` | 로그 감시 및 파이프라인 트리거 | Cron 또는 수동 |
| `diagnostic-agent.py` | OpenAI 기반 AI 진단 | log-watcher에서 호출 |
| `discord-alert.sh` | Discord 웹훅 알림 | log-watcher에서 호출 |
| `create-issue.sh` | GitHub Issue 자동 생성 | log-watcher에서 호출 |
| `create-pr.sh` | Auto-Fix PR 생성 | Issue 생성 후 호출 |
| `verify-fix.sh` | 수정 검증 (테스트 실행) | PR 생성 전 호출 |
| `rollback-fix.sh` | 실패 시 롤백 | 검증 실패 시 호출 |

---

## 환경 설정

### 로컬/EC2 (Cron 실행용)

`.env.monitoring` 파일:
```bash
# Discord 알림
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# OpenAI (진단 에이전트용)
OPENAI_API_KEY=sk-proj-...

# 진단 모델 (선택)
DIAGNOSTIC_MODEL=gpt-4o-mini
```

### GitHub Actions (CI/CD용)

| Secret | 용도 |
|--------|------|
| `OPENAI_API_KEY` | AI 진단 |
| `DISCORD_WEBHOOK_URL` | 에러 알림 |

---

## Cron 설정

현재 설정 (매일 오전 9시):
```bash
0 9 * * * cd /path/to/project && ./scripts/log-watcher.sh >> logs/ai-monitor.log 2>&1
```

설정 스크립트:
```bash
./scripts/setup-cron.sh
```

---

## 수동 테스트

### 전체 파이프라인 테스트
```bash
# 환경변수 로드 후 실행
source .env.monitoring && ./scripts/log-watcher.sh
```

### 개별 스크립트 테스트
```bash
# Discord 알림만 테스트
./scripts/discord-alert.sh "테스트 메시지"

# AI 진단만 테스트
python3 scripts/diagnostic-agent.py --error-code AI5002 --message "test error"
```

---

## 트러블슈팅

### 락 파일 문제
```bash
# 락 디렉토리 제거
rm -rf logs/.state/log-watcher.lock.lock
```

### 환경변수 미설정
```
ERROR: DISCORD_WEBHOOK_URL is not set
```
→ `.env.monitoring` 파일 확인 또는 `export` 명령으로 설정

### AI 진단 실패
```
Diagnostic failed, sending basic alert
```
→ `OPENAI_API_KEY` 확인, API 할당량 확인

---

## 비용

| 항목 | 예상 비용 |
|------|----------|
| OpenAI API (gpt-4o-mini) | ~$0.15/1M tokens |
| 월 예상 (100회 진단) | ~$5 |

---

## 관련 문서

- [AI 모니터링 개요](./00-overview.md)
- [AI 자동화 Overview](../ai-automation/overview.md)
- [보안 및 거버넌스](../ai-automation/security-governance.md)

---

## 변경 이력

| 날짜 | 변경 내용 |
|------|----------|
| 2026-02-03 | 초기 문서 작성, 파이프라인 운영 시작 |
